These are files that can be run on utah to generate the measurements of geocoronal Ha for each of the WHAM dark sky fields

the shadow_height_lib.py, lco_extinction.txt are also contained in the lvmdrp.  

The darkest_WHAM_list.csv files are needed because the WHAM fields are not always labeled in the headers
